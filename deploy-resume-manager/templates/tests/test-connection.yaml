apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deploy-resume-manager.fullname" . }}-test-connection"
  labels:
    {{- include "deploy-resume-manager.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deploy-resume-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
