import { CVNameSelectorPipe } from './cvname-selector.pipe';

describe('CVNameSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new CVNameSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
