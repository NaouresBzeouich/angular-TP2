import { TrucSympaDirective } from './truc-sympa.directive';

describe('TrucSympaDirective', () => {
  it('should create an instance', () => {
    const directive = new TrucSympaDirective();
    expect(directive).toBeTruthy();
  });
});
