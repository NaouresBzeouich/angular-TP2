apiVersion: apps/v1
kind: Deployment
metadata:
  name: angular-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: mon-app
  template:
    metadata:
      labels:
        app: mon-app
    spec:
      containers:
        - name: mon-app
          image: naouresbzeouich/resume-manager
          ports:
            - containerPort: 80
